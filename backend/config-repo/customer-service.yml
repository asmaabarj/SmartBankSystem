server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/customer_db
    username: postgres
    password: asmaa123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true